BORNTOWIN5 – ONLY 2 NEW CORRECTIONS

1. Referral link click opens the Registration page directly and keeps the Referral ID.
2. Registration page now has a required Email ID field; the email is sent to / stored for the existing email notification flow.

IMPORTANT:
- No database reset or clearing is included.
- Existing sample/test member IDs and data must be preserved.
- Keep the existing permanent PostgreSQL DATABASE_URL configuration.
- Do not delete or reset the database during deployment.
- The server.js in this package is the already verified 11-correction server with only the above member-page corrections applied.
