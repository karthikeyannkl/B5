BORNTOWIN5 Level Tracking FINAL ONLY CORRECTION V4

Today's corrections only:
1. Registration duplicate Joining PIN removed; only one Joining PIN field remains.
2. Member Profile menu now opens the actual Profile form (not Admin Messages). Profile includes personal + bank details and SAVE PROFILE.
3. Unified Member menu Level Tracking now shows only one item: Level Tracking Dashboard. Clicking it opens the existing Level Tracking page.
4. Admin Level Tracking payment section simplified: select upgrade request -> enter ONE receiver Member ID -> bank details auto-fill from server -> enter amount -> SEND PAYMENT DETAILS.
5. Member Level Tracking now receives assigned payment details directly from the server, so after Admin sends, the member sees the receiver/bank/amount details instead of only “Waiting for Admin”.
6. Existing design and other modules are preserved.
